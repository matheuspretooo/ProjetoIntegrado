package Model.DAO;

import java.util.ArrayList;

import Model.Entity.Usuario;

public class UsuarioDAO {
   /*ArrayList<Usuario> usuarioList;
        this.usuarioList = new ArrayList<Usuario>();
    }

    public void adicionarUsuario(Usuario usuario) {
        usuarioList.add(usuario);
      }

    public int obtemIndiceUsuario(Usuario usuario) {
        for (int i = 0; i < usuarioList.size(); i++) {
            Usuario usuarioReferencia = usuarioList.get(i);
            if (usuario.equals(usuarioReferencia)) {
                return i;
            }
        }
        throw new RuntimeException(" Usuario não encontrada");
    }
    
    public int getNumeroUsuario() {
        return usuarioList.size();
      }
    
    public void setUsuario(Usuario usuarioErrado, Usuario usuarioNovo) {
        for (int i = 0; i < usuarioList.size(); i++) {
            Usuario usuarioReferencia = usuarioList.get(i);
            if (usuarioErrado.equals(usuarioReferencia)) {
                usuarioList.set(i, usuarioNovo);
            }
    }
}

    public Usuario getUsuario(int indice) {
        return usuarioList.get(indice);
    }*/
}